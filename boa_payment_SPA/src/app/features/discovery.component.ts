import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-discovery',
  standalone: true,
  imports: [CommonModule],
  template: `
    <h2>Discovery</h2>
    <p>Discovery microservice UI placeholder.</p>
  `
})
export class DiscoveryComponent {}

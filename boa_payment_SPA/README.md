# BOA Payment SPA

Minimal Angular 18 standalone SPA demonstrating:

- Lazy-loaded standalone components for microservice features
- Auth using Angular signals and a `CanMatch` guard

To run:

```bash
cd boa_payment_SPA
npm install
npx ng serve --open
```
